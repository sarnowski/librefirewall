[package]
name = "pd-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
wire = { path = "../wire" }
queue = { path = "../queue" }
packet-buffer = { path = "../packet-buffer" }
