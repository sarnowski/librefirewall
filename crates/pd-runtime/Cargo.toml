[package]
name = "pd-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
wire = { path = "../wire" }
queue = { path = "../queue" }
packet-buffer = { path = "../packet-buffer" }
net-headers = { path = "../net-headers" }
routing = { path = "../routing" }
lfw-ip-endpoint = { path = "../ip-endpoint" }
# The arithmetic that turns a counter reading into an instant, which is what the
# transport's timers are stated against.
lfw-clock = { path = "../clock" }
lfw-log = { path = "../log" }

[dev-dependencies]
proptest = { workspace = true }
criterion = { workspace = true }

[lints]
workspace = true

[[bench]]
name = "route"
harness = false
