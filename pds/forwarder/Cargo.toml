[package]
name = "forwarder"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
sel4-microkit.workspace = true
pd-runtime = { path = "../../crates/pd-runtime" }
wire = { path = "../../crates/wire" }
lfw-log = { path = "../../crates/log" }
routing = { path = "../../crates/routing" }
# The verdict chain both directions are decided by. Held here rather than by
# either stage, because a stage of it may span both of them.
pipeline = { path = "../../crates/pipeline" }
lfw-metrics = { path = "../../crates/metrics" }

[lints]
workspace = true
